[LAGER]

[DEVENV]
image = lagerdata/devenv-cortexm
mount_dir = /app
shell = /bin/bash
cmd.setup = mkdir -p _build;cd _build; cmake .. -G Ninja -DCMAKE_TOOLCHAIN_FILE=../cmake/arm-gcc-toolchain.cmake;
cmd.build = cd _build;cmake --build .
cmd.clean = cd _build;ninja -t clean

